USE GRAPH ldbc_snb
SET syntax_version="v2"

CREATE OR REPLACE DISTRIBUTED QUERY OrderLimitOffset(INT k,INT j)
{
        forums = SELECT f 
            FROM Forum:f
            ORDER BY f.id
            LIMIT j,k;

        PRINT forums;
}